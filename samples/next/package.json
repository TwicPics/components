{
    "browserslist": [
        "> 1%",
        "last 2 versions",
        "not dead"
    ],
    "dependencies": {
        "@twicpics/components": "file:../../dist",
        "@twicpics/components-sample": "file:../_",
        "next": "12.1.6",
        "next-transpile-modules": "9.0.0",
        "react": "18.1.0",
        "react-dom": "18.1.0"
    },
    "license": "MIT",
    "scripts": {
        "build": "$npm_execpath install && next export",
        "dev": "$npm_execpath install && next -p 3000",
        "serve": "$npm_execpath install && next build && next start -p 3000"
    }
}
