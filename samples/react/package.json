{
    "browserslist": [
        "> 1%",
        "last 2 versions",
        "not dead"
    ],
    "dependencies": {
        "@twicpics/components": "file:../../dist",
        "@twicpics/components-sample": "file:../_",
        "react": "17.0.2",
        "react-app-rewired": "2.1.8",
        "react-dom": "17.0.2",
        "react-scripts": "4.0.3"
    },
    "license": "MIT",
    "scripts": {
        "build": "$npm_execpath install && DISABLE_ESLINT_PLUGIN=true SKIP_PREFLIGHT_CHECK=true react-app-rewired build",
        "serve": "$npm_execpath install && DISABLE_ESLINT_PLUGIN=true SKIP_PREFLIGHT_CHECK=true PORT=3000 react-app-rewired start"
    }
}
